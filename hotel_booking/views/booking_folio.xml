<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="folio_view_form" model="ir.ui.view">
            <field name="name">folio_view_form</field>
            <field name="model">booking.folio</field>
            <field name="arch" type="xml">
                <form string="folio_form">
                    <sheet>
                        <notebook>
                            <page name="lines" string="Lines">
                                <field name="line_ids">
                                    <tree create="0" edit="0" delete="0" editable="bottom">
                                        <field name="day" readonly="1"/>
                                        <field name="particulars" readonly="1"/>
                                        <field name="description" readonly="1"/>
                                        <field name="create_uid" widget="selection" readonly="1"/>
                                        <field name="payment_id" invisible="1" widget="selection" readonly="1"/>
                                        <field name="amount" sum="Balance" readonly="1"/>
                                        <field name="can_print_report" invisible="1"/>
                                        <button name="action_payment_folio" string="Print Payment" type="object" attrs="{'invisible': ['|',('can_print_report','=',True),('payment_id', '=', False)]}"/>
                                    </tree>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>
    </data>
</odoo>